package vidmot;

/** Represents the possible movement directions for a player. */
public enum Stefna {
    /** Move up. */
    UPP,
    /** Move down. */
    NIDUR,
    /** Move left. */
    VINSTRI,
    /** Move right. */
    HAEGRI,
    /** Mo movement. */
    KYRR,
}